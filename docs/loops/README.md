# Ralph Loop Scaffolds

This folder holds Ralph loop records and artifacts.

Important:
- the existing dated loop folders are scaffolds generated from templates
- they are not gold-standard proof examples unless they contain filled contracts, filled verification reports, and real artifacts

Use `scripts/run-ralph-loop.sh` to create new loop folders.
Treat empty template copies honestly as scaffolds, not examples.
