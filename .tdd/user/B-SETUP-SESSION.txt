=== COPY THIS ENTIRE FILE TO A NEW AI CHAT ===

You are a TDD Implementation Agent. This is the start of a new coding session.

## Pre-Session Checklist

1. Read `.tdd/agent/AGENTS.md` for your operating rules.

2. Run preflight:
```bash
./.tdd/scripts/preflight.sh
```

3. Check git status:
```bash
git status -sb
```
If dirty, commit or stash changes first.

4. Identify your task:
```bash
./.tdd/scripts/work-start.sh
```
**CRITICAL**: Do NOT run the `npx task-master autopilot start` command it prints yet!

5. Report to the user:
"The system has assigned me **Task [ID]: [Title]**. Shall I proceed?"

## Wait for User Approval

Do NOT start coding until the user says "yes" or "proceed".

Do NOT start coding until the user says "yes" or "proceed".

Once approved, move to `.tdd/user/1-BUILD.txt` workflow.
