=== COPY THIS TO A NEW AI CHAT (Fresh Session) ===

# 🛑 HUMAN CHEAT SHEET 🛑
Quick verification before you approve:

- [ ] **Ran Demo?** Did you run `npm run demo:[task]`?
- [ ] **Saw Proof?** Did you see the feature working (on screen or CLI)?
- [ ] **Signed Off?** Did you update the HITL file with your signature?

---

YOUR TURN: Human Verification
==============================

The agent says the task is done. Now YOU verify it actually works.

## Step 1: Open Your HITL Test

Find the file for this task:
  .tdd/output/hitl-verify/task-[ID].txt

## Step 2: Run the Checks

Follow the instructions in that file:
- Run the curl commands (copy-paste from terminal)
- Do the browser steps (go to URL, click, type, verify)
- Check each EXPECTED result

## Step 3: Mark Complete (If Pass)

If all checks pass, run:

  npx task-master set-status --id=[ID] --status=done

┌─────────────────────────────────────────────────────────────┐
│ NEXT: Copy '.tdd/user/B-SETUP-SESSION.txt' to start next   │
└─────────────────────────────────────────────────────────────┘

## If Something Is Broken

Tell the agent what failed. Example:
"Task [ID] failed verification. CHECK 2 failed: Expected 'Welcome, User' but saw 'Error 500'."

┌─────────────────────────────────────────────────────────────┐
│ NEXT: Tell the builder agent exactly what failed           │
└─────────────────────────────────────────────────────────────┘

The agent will fix it and you repeat the audit → approve cycle.

